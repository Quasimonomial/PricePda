<h2>This is the company index page</h2>

<ul>
	<% @companies.each do |company|%>
		<li>
			<%=link_to company.name, company_url(company)%>
		</li>
	<% end %>
</ul>

<%= link_to "New Company", new_company_url %>
<br>
<%=link_to "Root", root_url%>